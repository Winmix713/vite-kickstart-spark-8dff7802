import { useState, useMemo } from "react"; // Layout import PageHeader from '@layout/PageHeader'; import AppGrid from '@layout/AppGrid'; import WidgetGroup from '@components/WidgetGroup'; // Components import { AIChatInterface } from '@/components/ai-chat'; const AIChatWidget = ({ selectedTeams, onPredictionRequest }) => { return ( <WidgetGroup> <div className="rounded-2xl bg-card ring-1 ring-border overflow-hidden flex flex-col h-[600px]"> <div className="flex-1 overflow-hidden"> <AIChatInterface onPredictionRequest={onPredictionRequest} theme="dark" /> </div> {selectedTeams && ( <div className="p-4 border-t border-border bg-card/50"> <p className="text-sm text-muted-foreground"> Selected Match: <span className="font-semibold text-foreground">{selectedTeams.home} vs {selectedTeams.away}</span> </p> </div> )} </div> </WidgetGroup> ); }; const AIChatPage = () => { const[selectedTeams, setSelectedTeams] = useState(null); const handlePredictionRequest = (teams) => { setSelectedTeams(teams); }; const widgets = useMemo(() => ({ chat: <AIChatWidget selectedTeams={selectedTeams} onPredictionRequest={handlePredictionRequest} />, }), [selectedTeams]); return ( <> <PageHeader title="AI Chat" metaDescription="Interactive AI chat for predictions and analysis" /> <AppGrid id="ai_chat_page" widgets={widgets} /> </> ); }; export default AIChatPage;
