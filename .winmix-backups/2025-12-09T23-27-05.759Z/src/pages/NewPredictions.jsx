import { useMemo } from "react"; // Layout import PageHeader from '@layout/PageHeader'; import AppGrid from '@layout/AppGrid'; import WidgetGroup from '@components/WidgetGroup'; // Components import MatchSelection from '@/components/MatchSelection'; const MatchSelectionWidget = () => { return ( <WidgetGroup> <div className="rounded-2xl bg-card ring-1 ring-border overflow-hidden p-6"> <MatchSelection /> </div> </WidgetGroup> ); }; const NewPredictionsPage = () => { const widgets = useMemo(() => ({ matches: <MatchSelectionWidget />, }), []); return ( <> <PageHeader title="New Predictions" metaDescription="Create new predictions for upcoming matches" /> <AppGrid id="new_predictions_page" widgets={widgets} /> </> ); }; export default NewPredictionsPage;
