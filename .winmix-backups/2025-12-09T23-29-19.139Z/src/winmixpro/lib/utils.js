export const formatPercent = (value= 1)=> { return '${value.toFixed(decimals)}%'; }; export const formatShortNumber = (value)=> { if (value >= 1_000_000) { return '${(value / 1_000_000).toFixed(1)}M'; } if (value >= 1_000) { return '${(value / 1_000).toFixed(1)}K'; } return value.toString(); }; export const formatDuration = (milliseconds)=> { const seconds = Math.floor(milliseconds / 1000); const minutes = Math.floor(seconds / 60); const hours = Math.floor(minutes / 60); if (hours > 0) { return '${hours}ó ${minutes % 60}p'; } if (minutes > 0) { return '${minutes}p ${seconds % 60}mp'; } return '${seconds}mp'; }; export const formatRelativeTime = (minutes)=> { if (minutes < 60) { return '${minutes} perce'; } const hours = Math.floor(minutes / 60); if (hours < 24) { return '${hours} órája'; } const days = Math.floor(hours / 24); return '${days} napja'; }; export const formatDate = (date)=> { const d = typeof date === "string" ? new Date(date) ; return d.toLocaleDateString("hu-HU", { year: "numeric", month: "short", day: "numeric", }); }; export const formatTime = (date)=> { const d = typeof date === "string" ? new Date(date) ; return d.toLocaleTimeString("hu-HU", { hour: "2-digit", minute: "2-digit", }); }; export const formatDateTime = (date)=> { return '${formatDate(date)} ${formatTime(date)}'; }; export const truncateText = (text= "...")=> { if (text.length <= maxLength) return text; return text.slice(0, maxLength - suffix.length) + suffix; }; export const clamp = (value)=> { return Math.min(Math.max(value, min), max); }; export const debounce = <T extends (...args) => unknown>(fn) => { let timeoutId; return (...args) => { clearTimeout(timeoutId); timeoutId = setTimeout(() => fn(...args), delay); }; }; export const throttle = <T extends (...args) => unknown>(fn) => { let inThrottle; return (...args) => { if (!inThrottle) { fn(...args); inThrottle = true; setTimeout(() => (inThrottle = false), limit); } }; }; export const generateId = (prefix = "id")=> { return '${prefix}-${Date.now()}-${Math.random().toString(36).substring(2, 9)}'; }; export const cn = (...classes: (string | undefined | null | false)[])=> { return classes.filter(Boolean).join(" "); }; export const parseJSON = (jsonString)=> { try { return JSON.parse(jsonString); } catch { return fallback; } }; export const safeStringify = (value= "{}")=> { try { return JSON.stringify(value); } catch { return fallback; } }; export const isEmpty = (value)=> { if (value == null) return true; if (typeof value === "string") return value.trim().length === 0; if (Array.isArray(value)) return value.length === 0; if (typeof value === "object") return Object.keys(value).length === 0; return false; }; export const isValidEmail = (email)=> { const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/; return emailRegex.test(email); }; export const capitalizeFirst = (text)=> { if (!text) return text; return text.charAt(0).toUpperCase() + text.slice(1); }; export const slugify = (text)=> { return text .toLowerCase() .normalize("NFD") .replace(/[\u0300-\u036f]/g, "") .replace(/[^a-z0-9]+/g, "-") .replace(/^-+|-+$/g, ""); }; 