import React from 'react' import { useAuth } from '@contexts/AuthContext' import LoadingScreen from '@components/LoadingScreen' export function RoleGate({ children, allowedRoles = ['admin', 'analyst', 'user'] }) { const { profile, loading } = useAuth() if (loading) { return <LoadingScreen /> } if (!profile || !allowedRoles.includes(profile.role)) { return <Navigate to="/login" replace /> } return <>{children}</> } export default RoleGate 