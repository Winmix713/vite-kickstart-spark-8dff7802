// components import {animated, useSpring} from '@react-spring/web'; // hooks import {useInView} from 'react-intersection-observer'; // utils import PropTypes from 'prop-types'; const Spring = ({children, index = 1, className, type = 'fade', isCompact, ...props}) => { const[ref, inView] = useInView({ threshold: 0, triggerOnce: true }); const commonProps = { config, delay: 100 * index, } const fade = useSpring({ from, to, ...commonProps, ...props }) const slideLeft = useSpring({ from, to, ...commonProps, ...props }) const slideUp = useSpring({ from, to, ...commonProps, ...props }) const zoom = useSpring({ from, to, ...commonProps, ...props }) const spring = { fade, slideLeft, slideUp, zoom } return ( <animated.div className={className ? className : ''} style={spring[type]} ref={ref} {...props}> {children} </animated.div> ) } Spring.propTypes = { index: PropTypes.number, className: PropTypes.string, type: PropTypes.oneOf(['fade', 'slideUp', 'slideLeft', 'zoom']), children: PropTypes.node.isRequired } export default Spring 