import { supabase } from '@/integrations/supabase/client' type Product = Database['public']['Tables']['products']['Row'] export const productService = { async getAllProducts()= await supabase .from('products') .select('*') .order('created_at', { ascending) if (error) throw error return data || [] }, async getProductsByCategory(category)= await supabase .from('products') .select('*') .eq('category', category) .order('created_at', { ascending) if (error) throw error return data || [] }, async getProductById(id)= await supabase .from('products') .select('*') .eq('id', id) .single() if (error) { if (error.code === 'PGRST116') return null // No rows returned throw error } return data }, async getFeaturedProducts(limit = 10)= await supabase .from('products') .select('*') .eq('is_featured', true) .order('created_at', { ascending) .limit(limit) if (error) throw error return data || [] } }