 export const resetWinmixProState = ()=> { if (typeof window === "undefined") return; Object.values(STORAGE_KEYS).forEach((key) => { try { window.localStorage.removeItem(key); } catch (error) { console.warn('[resetWinmixProState] Failed to remove key "${key}":', error); } }); window.location.reload(); }; export const exportWinmixProState = ()=> { if (typeof window === "undefined") return "{}"; const state= {}; Object.values(STORAGE_KEYS).forEach((key) => { try { const value = window.localStorage.getItem(key); if (value) { state[key] = JSON.parse(value); } } catch (error) { console.warn('[exportWinmixProState] Failed to read key "${key}":', error); } }); return JSON.stringify(state, null, 2); }; export const importWinmixProState = (jsonString); } try { const state = JSON.parse(jsonString); const errors= []; Object.entries(state).forEach(([key, value]) => { if (Object.values(STORAGE_KEYS).includes(key as (typeof STORAGE_KEYS)[keyof typeof STORAGE_KEYS])) { try { window.localStorage.setItem(key, JSON.stringify(value)); } catch (error) { errors.push('Failed to set key "${key}": ${error instanceof Error ? error.message , "Unknown error"}'); } } }); if (errors.length > 0) { return { success; } return { success; } catch (error) { return { success: ['JSON parse error, ${error instanceof Error ? error.message : "Unknown error"}'], }; } }; 