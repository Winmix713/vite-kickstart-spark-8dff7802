 export const DEFAULT_GLASS_SETTINGS= { blur: "12px", opacity: 0.1, border: "1px solid rgba(255, 255, 255, 0.15)", shadow: "0 8px 32px rgba(0, 0, 0, 0.37)", }; export const PRESET_THEMES= [ { id: "aurora", name: "Aurora", description: "Emerald + indigó átmenet, admin fő téma", status: "stable", palette, glass, }, { id: "neon-ember", name: "Neon Ember", description: "Narancs árnyalat, piaci fókuszú lapokhoz", status: "experimental", palette, glass, }, { id: "fjord", name: "Fjord", description: "Kék-lila gradient, monitoring nézet", status: "stable", palette, glass, }, { id: "slate-glow", name: "Slate Glow", description: "Semleges, audit és riport lapok", status: "experimental", palette, glass, }, { id: "midnight-purple", name: "Midnight Purple", description: "Mély lila, AI modellezési felületekhez", status: "stable", palette, glass, }, { id: "forest-mint", name: "Forest Mint", description: "Természetes zöld, adatminőség lapokhoz", status: "experimental", palette, glass, }, ]; export const paletteToCSSVariables = (palette)=> ({ "--color-primary".primary, "--color-secondary".secondary, "--color-accent".accent, "--color-background".background, "--color-foreground".foreground, "--color-muted".muted, "--color-border".border, "--gradient-from".gradientFrom, "--gradient-to".gradientTo, }); export const glassSettingsToCSSVariables = (glass)=> ({ "--glass-blur".blur, "--glass-opacity".opacity.toString(), "--glass-border".border, "--glass-shadow".shadow, }); export const applyTheme = (theme)=> { if (typeof window === "undefined") return; const root = document.documentElement; const paletteVars = paletteToCSSVariables(theme.palette); const glassVars = glassSettingsToCSSVariables(theme.glass); Object.entries({ ...paletteVars, ...glassVars }).forEach(([key, value]) => { root.style.setProperty(key, value); }); }; export const exportThemeAsCSS = (theme)=> { const paletteVars = paletteToCSSVariables(theme.palette); const glassVars = glassSettingsToCSSVariables(theme.glass); const cssLines = Object.entries({ ...paletteVars, ...glassVars }).map(([key, value]) => ' ${key}: ${value};'); return '\n${cssLines.join("\n")}\n}'; }; export const exportThemeAsJSON = (theme)=> JSON.stringify(theme, null, 2); export const getThemeById = (id)=> PRESET_THEMES.find((theme) => theme.id === id); export const getDefaultTheme = ()=> PRESET_THEMES[0]; 